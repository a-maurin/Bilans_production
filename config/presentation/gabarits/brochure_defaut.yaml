# ========================================================================================
# ALIAS RÉTROCOMPATIBILITÉ : BROCHURE DEFAULT (`brochure_defaut.yaml`)
# ========================================================================================
#
# RÔLE : Gabarit de PRÉSENTATION uniquement (mise en page, ordre des sections, titres visuels).
# → Ce fichier ne contient AUCUNE logique métier (filtres, sources de données, périmètre géo).
# → Pour la logique métier, voir : config/profils_bilan/<profil>.yaml
#
# Relation Profil / Gabarit :
#   - Le PROFIL définit QUOI analyser : périmètre géo, sources, agrégations, options métier.
#   - Le GABARIT définit COMMENT présenter : grille de pages, widgets, ordre des chapitres.
#
# Note : Ce fichier est un alias de rétrocompatibilité vers `gabarit_defaut.yaml`.
# ========================================================================================

version: 1
gabarit_id: brochure_defaut
alias_of: gabarit_defaut
label: "Gabarit institutionnel par défaut (Alias)"
description: "Gabarit de présentation synthétique institutionnelle par défaut de l'OFB"

cible: les_deux

title:
  line2_mode: fixed
  line2_fixed: "Bilan Synthétique Institutionnel"

sections:
  order:
    - sec1
    - sec2
    - sec4
    - sec3
    - sec6

cartographie:
  items_masques_brochure:
    - titre_principal
    - sous_titre
    - bandeau_titre
    - bandeau_logos_ofb
    - logo_ofb_bas_droite
    - bandeau_source

layout_grid:
  type: grid
  pages:
    - page_number: 1
      rows:
        - columns:
            - width: "100%"
              widget:
                type: stat_kpi_grid
        - columns:
            - width: "50%"
              widget:
                type: map
            - width: "50%"
              widget:
                type: section_group
                sections: [sec1, sec2]
    - page_number: 2
      rows:
        - columns:
            - width: "50%"
              widget:
                type: theme_breakdown_table
            - width: "50%"
              widget:
                type: evolution_chart
